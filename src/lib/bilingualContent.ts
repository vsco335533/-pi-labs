export function getBilingualText(text: string, language: string): string {
    // This is a placeholder. In a real app, `text` might be a JSON string or object {en: '...', hi: '...'}
    // For now, simply return the text as-is since categories are currently just strings.
    // If you implement bilingual database fields later, expand this logic.
    return text;
}
